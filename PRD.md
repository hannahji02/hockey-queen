# 핀볼 룰렛 웹앱 기획서 v1.1

## 1. 프로젝트 개요

- **프로젝트명**: Hockey Puck Roulette (가칭)
- **목적**: 아이스하키 동호회 워크샵에서 사용할 추첨 룰렛 웹앱
- **컨셉**: 수직으로 세운 아이스링크 위에서 퍽들이 떨어지며 장애물을 거쳐 1등(최초 도착) 또는 꼴등(최후 도착) 당첨자를 결정
- **배포**: GitHub → Vercel
- **스택**: Next.js (App Router) + TypeScript + Matter.js(물리엔진) + Canvas

### v1.0 → v1.1 변경 요약
- 레이아웃 3분할 구조로 변경 (우측 실시간 순위 패널 추가)
- 다크모드 디폴트 확정
- 시작/셔플 버튼 분리
- 결승선 직전 페이스오프 서클 깔때기 구조 추가
- 당첨자 화려/축제형 애니메이션 명세 신규 추가 (10번 섹션)

---

## 2. 화면 구성

### 2.1 메인 레이아웃 (3분할 구성)

```
┌─────────────────────────────────────────────────────────┐
│  [상단 헤더] 로고 / 타이틀                                  │
├──────────────┬──────────────────────────┬───────────────┤
│              │                          │               │
│  [좌측 패널]  │     [중앙 게임 캔버스]      │ [우측 순위패널] │
│              │                          │               │
│  - CSV 업로드 │   - 세로형 아이스링크       │  실시간 도착    │
│  - 참가자 리스트│  - 퍽 낙하 애니메이션      │  순위 리스트    │
│   (체크박스)  │   - 장애물 배치            │               │
│  - 모드 선택  │   - 페이스오프 깔때기       │  - 색상 매칭    │
│   (1등/꼴등)  │   - 골인존(하단)          │  - 순번 표시    │
│  - 셔플 버튼  │                          │  - 자동 스크롤  │
│  - 시작 버튼  │                          │               │
│  - 리셋 버튼  │                          │               │
│  - 당첨이력   │                          │               │
│              │                          │               │
└──────────────┴──────────────────────────┴───────────────┘
```

### 2.2 반응형 및 테마
- 데스크탑 우선 (워크샵 빔프로젝터 사용 가정)
- 최소 1280x720 지원
- **다크모드 디폴트** (네온 컬러 가시성 극대화)
- 라이트모드 토글 제공 (옵션)

---

## 3. 기능 명세

### 3.1 참가자 관리
- **CSV 업로드**: 첫 컬럼 = 이름 (헤더 유무 자동 감지)
- **이름 제한**: 3자 초과 시 자동 절단 + 경고 표시
- **체크박스 리스트**: 디폴트 전원 체크 ON
- **새로고침**: 체크 상태 + 당첨 이력 모두 초기화
- **참가자 수 제한**: 최대 30명 (초과 시 업로드 거부)

### 3.2 모드 선택
- **라디오 버튼**: 1등 당첨 / 꼴등 당첨
- **디폴트**: 꼴등 당첨 (마지막 도착자)
- 매 라운드 시작 전 변경 가능

### 3.3 셔플 & 시작 (버튼 분리)
- **셔플 버튼**: 퍽들의 시작 X좌표 무작위 재배치 (시각적으로 섞이는 애니메이션)
- **시작 버튼**: 셔플 결과를 기반으로 낙하 시작
- 셔플 없이 바로 시작 시: 자동으로 1회 셔플 후 시작

### 3.4 추첨 로직 (핵심)
- **완전 랜덤** 물리 시뮬레이션 (Matter.js)
- **중복 당첨 방지**: 전원이 1회씩 당첨될 때까지 기당첨자 제외
- **사이클 관리**:
  - 20명 중 5명 당첨 → 잔여 15명 풀에서 추첨
  - 20명 전원 당첨 완료 → 풀 리셋, 새 사이클 시작
- **실행 방식**: 활성 참가자(체크된 인원) 중 미당첨자만 퍽으로 생성 → 동시 낙하

### 3.5 결과 처리
- 1등 모드: 최초 골인존 도착 퍽
- 꼴등 모드: 최후 골인존 도착 퍽
- 당첨 즉시 화려/축제형 애니메이션 발동 (10번 섹션 참고)
- 당첨 이력 좌측 패널에 누적 표시 (사이클 번호 함께)

### 3.6 실시간 순위 패널 (신규)
- 우측 패널에 도착 순서대로 참가자 이름 실시간 누적
- 각 이름 좌측에 해당 퍽의 색상 점(dot) 표시
- 순위 번호(#1, #2 ...) 자동 부여
- 인원 많을 시 자동 스크롤
- 1등 모드: 최상단 강조 / 꼴등 모드: 최하단 강조

---

## 4. 비주얼 디자인 (아이스하키 테마)

### 4.1 퍽 (참가자 표현)
- **형태**: 검은색 원형 입체 (그라데이션 + 측면 두께감)
- **이름 표시**: 퍽 윗면 중앙, 한글 3자 이내
- **이름 색상**: 참가자별 랜덤 컬러 (네온톤: 시안, 마젠타, 옐로우 등)
- **물리 속성**: 반발 0.6, 마찰 0.05 (얼음 표현)

### 4.2 배경 (아이스링크)
- 다크 베이스 + 연한 하늘색 빙판 그라데이션
- 좌우 측면: 보드(목재 텍스처) + 광고판 라인
- 골 라인(빨강), 페이스오프 서클(파랑) 등 링크 마킹

### 4.3 장애물 (placeholder 단계)
| 요소 | 기획 단계 표현 | 향후 교체 이미지 |
|------|---------------|----------------|
| 코치진 얼굴 몬스터 | 빨간색 원형 (고정/회전) | 코치 얼굴 PNG |
| 골대 | 파란색 사각 프레임 (퍽 일시 트랩) | 골대 PNG |
| 휘둘리는 스틱 | 회색 부채꼴 (왕복 회전) | 스틱 PNG |
| 스케이팅 선수 | 갈색 사각 + 양옆 날 형태 막대 | 선수 몸통 + 합성 얼굴 PNG |

### 4.4 장애물 상호작용
- **골대**: 진입 시 일정 시간(1~2초) 갇혔다 배출, 일정 확률로 튕겨나옴
- **스틱**: 부채꼴 회전 운동, 충돌 시 각도 변형 + 속도 감소
- **스케이트 날**: 좌우로 살짝 진동, 강한 반발력
- **코치 몬스터**: 정적 또는 천천히 회전, 일반 반사

### 4.5 결승선 직전 페이스오프 깔때기 (신규)
- 골인존 바로 위에 페이스오프 서클 도트 패턴 배치
- 좁아지는 깔때기 형태로 퍽 흐름 집중
- 도트들이 마지막 무작위성 부여 → 예측 불가능한 결승

---

## 5. 확률 보정 메커니즘

- 본 시스템은 **물리 기반 의사 랜덤**
- 진정한 균등 확률은 보장 안 됨 (위치에 따른 편향 존재)
- **완화 장치**:
  - 매 라운드 퍽의 시작 X좌표를 랜덤 셔플
  - 초기 진입 속도에 미세 노이즈 추가
  - 중복 당첨 방지 로직으로 장기 균등성 보장
  - 페이스오프 깔때기로 결승 직전 변동성 강화

### 5.1 히든 순위 보정 로직 (Phase 3에서 활성화)

특정 참가자(`lib/roulette/excluded.ts` 의 `HIDDEN_RANK_ADJUSTED` 명단)는
다음과 같이 처리됨:

- 퍽으로 **정상 생성** 및 **정상 낙하** (UI 상 일반 참가자와 동일)
- 결승선 직전(트랙 잔여 거리 15% 이내)에서 도착 순위 모니터링
- **상위 25% 진입 예정** → 속도 70%로 감속 (스틱 충돌처럼 자연스럽게 연출)
- **하위 25% 진입 예정** → 속도 140%로 가속 (밀어주는 듯한 연출)
- **중위 50% 범위 내** → 보정 없이 자연 낙하
- 결과: 1등/꼴등에 절대 선정되지 않음, 외부 노출 방지

관련 상수:
- `SAFE_ZONE_RATIO = 0.25`
- `ADJUSTMENT_FORCE.decelerate = 0.7`
- `ADJUSTMENT_FORCE.accelerate = 1.4`
- `ADJUSTMENT_TRIGGER_DISTANCE_RATIO = 0.15`

---

## 6. 기술 스택 & 라이브러리

| 항목 | 선택 |
|------|------|
| 프레임워크 | Next.js 14+ (App Router) |
| 언어 | TypeScript |
| 물리엔진 | Matter.js |
| 렌더링 | HTML5 Canvas (Matter.js Render 또는 커스텀) |
| 상태관리 | React useState/useReducer |
| CSV 파싱 | PapaParse |
| 스타일 | Tailwind CSS |
| 애니메이션 | Framer Motion |
| 파티클 | canvas-confetti |
| 배포 | Vercel |

---

## 7. 디렉토리 구조 (예정)

```
hockey-puck-roulette/
├── app/
│   ├── page.tsx                (메인 페이지)
│   └── layout.tsx
├── components/
│   ├── ParticipantPanel.tsx    (좌측 패널)
│   ├── GameCanvas.tsx          (중앙 캔버스)
│   ├── RankingPanel.tsx        (우측 실시간 순위)
│   ├── ModeSelector.tsx
│   └── WinnerOverlay.tsx       (당첨 애니메이션)
├── lib/
│   ├── physics/
│   │   ├── engine.ts           (Matter.js 셋업)
│   │   ├── puck.ts             (퍽 생성)
│   │   ├── obstacles.ts        (장애물 생성)
│   │   └── stage.ts            (스테이지 레이아웃)
│   ├── roulette/
│   │   ├── pool.ts             (당첨 풀 관리)
│   │   └── csv.ts              (CSV 파싱)
│   ├── animation/
│   │   ├── confetti.ts         (컨페티 연출)
│   │   └── winner.ts           (당첨 시퀀스)
│   └── constants.ts
├── public/
│   └── assets/                 (향후 이미지 자산)
├── package.json
└── README.md
```

---

## 8. 개발 단계

1. **Phase 1**: Next.js 프로젝트 골격 + 3분할 UI 레이아웃 (정적, 다크모드)
2. **Phase 2**: CSV 업로드 + 참가자 관리 + 셔플/시작 버튼
3. **Phase 3**: Matter.js 물리엔진 연결 + 퍽 낙하 (장애물 없음)
4. **Phase 4**: Placeholder 장애물 + 페이스오프 깔때기 + 충돌 인터랙션
5. **Phase 5**: 1등/꼴등 판정 + 중복 방지 풀 로직 + 실시간 순위 패널
6. **Phase 6**: 화려/축제형 당첨 애니메이션 (10번 섹션)
7. **Phase 7**: 디자인 폴리싱
8. **Phase 8**: 이미지 자산 교체 (사용자 업로드 후)
9. **Phase 9**: Vercel 배포

---

## 9. 사용자 작업 필요 항목

- GitHub 저장소 생성 (사용자 직접)
- Vercel 계정 + GitHub 연동 (사용자 직접)
- 코치진 얼굴/골대/스틱/선수 이미지 (PNG, 투명배경 권장)
- CSV 파일 (이름 1컬럼)

---

## 10. 당첨자 화면 애니메이션 (화려/축제형)

### 10.1 3단 시퀀스 구성

**1단계: 골인 순간 (0~1초)**
- 당첨 퍽이 골인존 통과 시 **슬로우모션 0.3배속** 진입
- Matter.js `engine.timing.timeScale = 0.3` 동적 조정
- 화면 카메라 **줌인** → 당첨 퍽 클로즈업
- **모드별 차별 연출**:
  - 1등 모드: 골대 그물 출렁임 + **빨간 골 라이트** 점멸 (NHL 골 연출)
  - 꼴등 모드: 화면 톤 다운 후 마지막 퍽 강조

**2단계: 당첨 발표 (1~4초)**
- 전체 화면 **암전** + 당첨 퍽에 **스포트라이트** 조명
- 당첨 퍽이 화면 중앙으로 **떠오르며 회전 + 글로우 발광**
- 중앙에 당첨자 이름 **대형 텍스트 등장**
  - Framer Motion 바운스 (scale 0 → 1.2 → 1.0)
  - 텍스트 컬러 = 해당 퍽 컬러로 통일
- **canvas-confetti 컨페티 폭발**
  - 좌우 양쪽에서 비대칭 발사
  - 팀 컬러 + 네온 컬러 혼합
  - 약 200개 파티클, 3초간 낙하
- 사운드 (옵션, v2): 하키 골 혼 + 관중 환호

**3단계: 정착 & 액션 (4초 이후)**
- 이름 텍스트 상단으로 이동 후 정착
- 우측 당첨 이력에 페이드인으로 자동 추가
- **"다시 돌리기"** + **"닫기"** 버튼 등장
- 컨페티 천천히 낙하 후 페이드아웃

### 10.2 모드별 차별화 요약

| 단계 | 1등 당첨 | 꼴등 당첨 |
|------|---------|---------|
| 골인 순간 | 골대 그물 흔들림 + 빨간 골 라이트 | 화면 톤 다운 + 스포트라이트 |
| 사운드 톤 | 환희, 골 혼 풀볼륨 | 드라마틱, 톤 낮춤 |
| 컨페티 색상 | 골드 + 팀 컬러 | 실버 + 네온 |

### 10.3 기술 구현 요점

- **CSS Transition + Canvas Overlay** 조합
- 컨페티: `canvas-confetti` (경량, 무설정)
- 슬로우모션: `engine.timing.timeScale` 일시 조정 후 복귀
- 텍스트 애니메이션: Framer Motion variants
- 스포트라이트: SVG radial gradient mask
- 글로우: CSS `box-shadow` + `filter: drop-shadow`

---

## 11. 추후 확장 가능 항목 (v2 후보)

- 사운드 효과 (충돌, 골 혼, 관중 환호)
- 녹화 기능 (워크샵 영상 보존)
- 카메라 추적 (선두 퍽 자동 포커싱)
- 모바일 반응형
- 결과 CSV 다운로드
- 당첨 확률 가중치 설정 (특정 인원 ↑)
- 다인 당첨 모드 (상위/하위 N명)
